import React from 'react';
import Headerlist from './headerlist/Headerlist';
const Header = () => {
    return (
        <header>
            <Headerlist />
        </header>
    );
}

export default Header;